import * as Redis from 'ioredis';
import { RedisClientOptions, RedisModuleOptions } from './redis.interface';
import { Provider } from '@nestjs/common';
export declare class RedisProvider {
    static createClient(options: RedisClientOptions): Redis.Redis;
    static init(options: RedisModuleOptions[]): Provider[];
    private static createRedisClientProvider;
}
